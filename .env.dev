# Development Mode Configuration
# This file contains settings for local development without Google OAuth

# Backend - Skip authentication
DEV_MODE=true

# Frontend - Use dev mode authentication
VITE_DEV_MODE=true

# API URL for frontend to backend communication
VITE_API_URL=http://localhost:8001

# Dummy values for Google OAuth (not used in dev mode but may be checked by build)
VITE_GOOGLE_CLIENT_ID=dev-mode-client-id
GOOGLE_CLIENT_SECRET=dev-mode-secret
VITE_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# PostgreSQL Configuration (optional - uses defaults if not set)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=protodb